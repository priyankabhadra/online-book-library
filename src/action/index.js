import { createAction } from 'redux-actions'

export const addBook = createAction('addBook');

export const deleteBook = createAction('deleteBook');

export const editBook = createAction('editBook');

